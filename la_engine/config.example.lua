local Config = {
    Enable = true,
    Debug = false,
    StatusCommand = 'la_engine_status',
    logger = nil,
    controllers = {
        eraTraffic = true,
        weather = true
    }
}

return Config
